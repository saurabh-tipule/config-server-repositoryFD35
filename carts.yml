spring:
  application:
    name: cart-service
  datasource:
    driver-class-name: com.oracle.Driver
    password: root
    url: jdbc:mysql://localhost:3306/FD35
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: false
    show-sql: false
